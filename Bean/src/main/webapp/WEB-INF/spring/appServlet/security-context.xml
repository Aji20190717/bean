<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/security
								 http://www.springframework.org/schema/security/spring-security.xsd
								 http://www.springframework.org/schema/beans
								 http://www.springframework.org/schema/beans/spring-beans.xsd
								 http://www.springframework.org/schema/context
								 http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<context:component-scan
		base-package="com.power.security" />

	<http auto-config="true" use-expressions="true">

		<!-- 자원 관련 url -->
		<intercept-url pattern="/resources/**"
			access="permitAll" />

		<!-- main page 관련 url -->
		<intercept-url pattern="/index.jsp" access="permitAll" />
		<intercept-url pattern="/header.jsp" access="permitAll" />
		<intercept-url pattern="/main.do" access="permitAll" />
		<intercept-url pattern="/mainpage.jsp"
			access="permitAll" />
		<intercept-url pattern="/intro.do" access="permitAll" />

		<!-- 로그인/회원가입 관련 url -->
		<intercept-url pattern="/loginform.do"
			access="permitAll" />
		<intercept-url pattern="/registtype.do"
			access="permitAll" />
		<intercept-url pattern="/resister.do" access="permitAll" />
		<intercept-url pattern="/idChk.do" access="permitAll" />
		<intercept-url pattern="/emailChk.do" access="permitAll" />
		<intercept-url pattern="/registform.do"
			access="permitAll" />
		<intercept-url pattern="/logininfo.do"
			access="permitAll" />
		<intercept-url pattern="/**" access="permitAll" />

		<form-login login-page="/loginform.do"
			login-processing-url="/logininfo.do" default-target-url="/login.do"
			username-parameter="member_id" password-parameter="member_pw"
			authentication-failure-url="/login.do?error=1"
			always-use-default-target="true" />

		<logout invalidate-session="true"
			logout-url="/logoutSecurity.do" logout-success-url="/logout.do"
			delete-cookies="true" />

		<csrf disabled="true" />

		<access-denied-handler error-page="/error.do" />

	</http>

	<authentication-manager>
		<authentication-provider
			user-service-ref="securityService">
			<password-encoder ref="passwordEncoder" />
		</authentication-provider>
	</authentication-manager>

	<beans:bean id="passwordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

</beans:beans>